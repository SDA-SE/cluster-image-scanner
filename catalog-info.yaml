apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ClusterImageScanner
  description: |
    Discover vulnerabilities and container image misconfiguration in production environments.
  tags:
    - security-stack
  annotations:
    sda.se/summary: |
      Discover vulnerabilities and container image misconfiguration in production environments.
    sda.se/federation-scopes: public
    backstage.io/techdocs-ref: url:https://github.com/SDA-SE/clusterscanner-orchestration/tree/master
spec:
  type: service
  owner: security-journey
  system: cluster-image-scanner
  lifecycle: production
